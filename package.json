{
  "name": "al-sql",
  "version": "1.1.4",
  "description": "Abstraction_Layer-Sql is a sql abstraction layer that can be used for every sql database to perform simple querys.",
  "main": "dist/index.js",
  "scripts": {
    "compile": "tsc -p .",
    "testi": "npm up -g npm@latest; npm ci; npm run test",
    "test": "mocha -r ts-node/register 'test/**/*.test.ts'",
    "release": "npm up -g npm@latest; npm up; npm run compile; npm run test; npm publish"
  },
  "repository": {
    "url": "git@github.com:HalsMaulMajo/al-sql.git",
    "type": "git"
  },
  "author": "HalsMaulMajo",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4",
    "@types/mocha": "^8",
    "@types/node": "^16",
    "chai": "^4",
    "mocha": "^9",
    "nodemon": "^2",
    "pg": "^8.7.1",
    "ts-node": "^10.4.0",
    "typescript": "^4"
  },
  "dependencies": {
    "colors": "^1.4.0"
  }
}